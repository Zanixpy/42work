Today I'm gonna start the ft_printf function. For this project I need to master the concept of variadic function but this is good. So now I'm gonna observe the behavior of 
the original printf to see how my printf must react.

Following conversions for my printf :

- %c : ft_putchar_fd
- %s : ft_putstr_fd
- %p : Base 16 conversion
- %d : ft_itoa
- %i : ft_itoa
- %u : ft_itoa
- %x : Base 16 conversion
- %X : Base 16 conversion
- %% : ft_putchar_fd

Original printf behavior :

When we compile with CC -WWW, errors showed up but with only CC that compiled. So i don't need to worry about,
For me to create the new string, when i find a conversion sign, I need to catch the beginning with substr, convert the variable in string and do a strjoin with the string before
the conversion sign. When I did this I can strjoin the beginning with the next of the string.

So I need to think like a computer.

So let's divide the main problems onto sub-problems.

### Decomposition :

Main problem : Create a function that mimic printf
Printf : A function that can use conversion sign to display a string with conversion sign replaced by the variables. It's return 0 on success and >0 if an error occurs.

If I decompose the original function with all tools I got. I will say 
- Read the const string. 
- Find format sign.
- Replace the format sign by the variable in string
- Display the final string with all changes.

### Pattern Recognition :

Pattern identified : reading data of const string / Solution : a while loop until '\0'
Pattern identified : Searching for a format sign (%.)
Pattern identified : Replacing format sign / Solution : use of substr and strjoin

### Abstraction

No-essential details : inputs of people
Essential : format string
Abstraction : Just converting when i found a format string

### Decomposition into sub-problems :

int ft_printf(const char *format, ...) : Read the const string / return 0 or >0 if an error.
char ft_search_format(const char *format) : Find format sign. / return the letter of format
ft_putchar_fd / ft_itoa (base 10 and 16) / ft_putstr_fd : converting variable into string

### Data structuring :

va_list ap : contains args from variadic function
unsigned char *var : contains arg non converted in string by the appopriate function
char *var_convert : contains arg converted in string
char *final : contains the string with all changes
int read_format : incremetent the format string.


